# Server
PORT=5000
TEMP_DIR=temp
JOBS_DIR=jobs

# OpenAI (if used by summarization service)
# OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-4o-mini

# yt-dlp configuration
# Specify the JS runtime for YouTube decryption (EJS). Defaults are applied automatically,
# but setting these can resolve "No supported JavaScript runtime" errors.
YTDLP_JS_RUNTIME_NAME=node
# On Windows, this usually is C:\\Program Files\\nodejs\\node.exe
# On macOS/Linux, something like /usr/local/bin/node
# Example:
# YTDLP_JS_RUNTIME_PATH=C:\\Program Files\\nodejs\\node.exe

# Optional: use Android player client to mitigate age-gating
YTDLP_EXTRACTOR_ARGS=youtube:player_client=android

# Provide cookies if you need to access age-restricted/private videos.
# Option 1: Read from a local browser profile
# Valid values: chrome | edge | firefox
# YTDLP_COOKIES_FROM_BROWSER=chrome

# Option 2: Provide a cookies.txt file
# YTDLP_COOKIES_FILE=C:\\path\\to\\cookies.txt
