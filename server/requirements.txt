# Requirements for server-side transcription and audio handling
# Install a compatible `torch` wheel separately (see README) to avoid platform issues.
openai-whisper>=2023.6.1
ffmpeg-python>=0.2.0
numpy>=1.23

# Optional: PyTorch (install the correct wheel for your platform from https://pytorch.org/)
# Example (CPU):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
