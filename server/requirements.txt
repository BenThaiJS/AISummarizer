# Whisper for transcription
openai-whisper

# Faster audio processing
ffmpeg-python

# Optional: for better performance, GPU support, install PyTorch with CUDA if available
torch

# You may also need numpy
numpy
